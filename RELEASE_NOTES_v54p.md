# v54p — Global Home Timer

Single functional change: Home now includes a Timer shortcut that opens the existing Focus Timer as a standalone timer. The Today's Focus timer remains unchanged.

No data schema, recurring-task, project, birthday, Home ordering, Search, Timeline or storage behaviour was changed.
