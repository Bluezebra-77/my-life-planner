# v54q — Project Step Reordering

One functional change only: project steps can now be moved up or down from their three-dot menu.

Reordering changes only the step sequence. It does not change due dates, completion state, notes, or project data. The project Next step recalculates from the saved order and Home/Timeline are refreshed.
